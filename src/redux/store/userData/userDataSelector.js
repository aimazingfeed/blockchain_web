
export const userDataAdressSelector =
  (state) => state.userData.adress;

export const userDataBalanceSelector =
    (state) => state.userData.balance;

export const userDataIsConnectedSelector =
    (state) => state.userData.isConnected;